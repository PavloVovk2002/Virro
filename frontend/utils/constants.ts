// frontend/utils/constants.ts

import { Platform } from 'react-native';

// 🚀 Use your local IP for all cases when testing on physical devices or emulator
const LOCAL_IP = '192.168.4.65'; // Replace with your local IP

export const BASE_URL = `http://${LOCAL_IP}:3001`;

// Optional: add a production URL later if needed
